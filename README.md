VL53L5CX library code is executed in arduino for ESP32C3 with no modification.
for python gui opencv library, numpy  and serial libraries are needed install through pip (search gpt)
camserver ip needs to be modified. ip :80/stream will stream continous video in browser and ip/capture captures still image
